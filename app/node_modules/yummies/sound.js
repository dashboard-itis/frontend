const playSound = async (file, { volume = 1 } = {}) => {
  let audio = new Audio(file);
  audio.volume = volume;
  audio.muted = !volume;
  await audio.play();
  audio.remove();
  audio = null;
};
export {
  playSound
};
//# sourceMappingURL=sound.js.map
