import { ReactElement } from 'react'
// TODO: реализация логики ролевого роута
type Props = {
  children: ReactElement
}

export const PrivateRoute = ({ children }: Props): ReactElement => {
  return children
}
